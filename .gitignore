node_modules/
dist/
.wrangler/
.cfstate/
.dev.vars
.env

# Per-repo gh credential store for the dsh delegate (GH_CONFIG_DIR).
# Holds this repo's fine-grained PAT; never commit it.
.gh/
.DS_Store
worker-configuration.d.ts
.npm-cache/
*.tsbuildinfo
.playwright-browsers/
.r1-screenshots/

# Local Claude Code settings (may contain the repo-scoped GH token)
.claude/settings.local.json
